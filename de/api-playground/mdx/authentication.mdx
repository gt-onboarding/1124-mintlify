---
title: "Authentifizierung"
description: "Sie können Authentifizierungsparameter festlegen, damit Nutzer ihre echten API-Schlüssel verwenden."
---

<div id="enabling-authentication">
  ## Authentifizierung aktivieren
</div>

Sie können in Ihrer `docs.json` eine Authentifizierungsmethode hinzufügen, um sie global für alle Seiten zu aktivieren, oder Sie legen sie seitenweise fest.

Die Authentifizierungsmethode einer Seite überschreibt die globale Methode, wenn beide festgelegt sind.

<div id="bearer-token">
  ### Bearer-Token
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "bearer"
      }
    }
}
```

```mdx Page Metadata
---
title: "Seitentitel"
authMethod: "bearer"
---
```

</CodeGroup>

<div id="basic-authentication">
  ### Basic-Authentifizierung
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "basic"
      }
    }
}
```

```mdx Page Metadata
---
title: "Seitentitel"
authMethod: "basic"
---
```

</CodeGroup>

<div id="api-key">
  ### API-Schlüssel
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "key",
        "name": "x-api-key"
      }
    }
}
```

```mdx Seitenmetadaten
---
title: "Seitentitel"
authMethod: "key"
---
```

</CodeGroup>

<div id="none">
  ### none
</div>

Die Authentifizierungsmethode „none“ ist hilfreich, um die Authentifizierung für einen bestimmten Endpoint zu deaktivieren, nachdem ein Standard in docs.json festgelegt wurde.

<CodeGroup>
```mdx Page Metadata
---
title: "Your page title"
authMethod: "none"
---
```
</CodeGroup>